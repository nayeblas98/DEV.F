// Concatenar los siguientes arrays

var animals = ['eagle', 'parrot', 'monkey', 'boar', 'lion'];
var comingSoonAnimals = ['panter', 'dragon', 'turtle'];
var newAnimalsList = animals.concat(comingSoonAnimals);

console.log(newAnimalsList)

// Agregar 'cow' al arreglo animals

animals.push('cow');
console.log(animals);

// Quitar 'eagle' al arreglo animals

animals.shift();
console.log(animals);

// Ordenar el arreglo de menor a mayor

var arr = [4, 6, 1, 0, 8, 2];
arr.sort();

console.log(arr);

